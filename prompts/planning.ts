/**
 * Planning Prompt Template
 * (Retained for reference/compilation but no longer active; resolution is programmatic).
 */
export function planningPrompt(companyName: string): string {
  return `Refined equity research guidelines for planning the structural profile mapping of: ${companyName}.`;
}
