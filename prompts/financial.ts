/**
 * Financial Analysis Prompt Template
 * (Retained for reference/compilation but no longer active; service query is programmatic).
 */
export function financialPrompt(companyName: string): string {
  return `Equity research instruction set for evaluating balance sheet and cash flow health of: ${companyName}.`;
}
